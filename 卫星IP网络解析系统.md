# 卫星IP网络解析系统 \- 纯UI极简字段交付表（最终开发版）

**说明：仅保留字段名、字段类型、状态枚举、组件类型，无冗余文案，可直接UI建模、前端枚举、接口对接**

## 1\. 全局通用枚举（全局复用）

### 1\.1 数据类型标签枚举

- 网络协议、IP端口、域名、邮箱、账号口令、SSL证书、IMSI、音频、视频

### 1\.2 风险等级枚举

- 高危、中危、低危、无害

### 1\.3 解析状态枚举

- 解析中、解析完成、解析失败、数据残缺、疑似伪造

### 1\.4 全局基础通用字段

- 采集时间戳\(ms\)、卫星任务ID、波束编号、链路频段、解析状态、置信度\(0\-100\)、风险等级、溯源ID

## 2\. 顶部筛选栏组件字段

- 时间模式\(单选\)、时间区间\(时间选择器\)、卫星链路筛选\(下拉\)、数据类型\(多选\)、风险等级\(多选\)、全局关键词检索\(输入框\)、刷新、导出、快照、溯源

## 3\. 态势总览页 UI 字段

### 3\.1 统计卡片字段

- 总数据量、高危数据量、IP端口映射数、账号口令组数、IMSI特征数、音视频样本数

### 3\.2 图表绑定字段

- 数据类型分布、风险等级分布、协议占比、IP属地热力

### 3\.3 高危TOP列表字段

- 风险标签、数据类型、核心内容、风险等级、解析时间、操作

## 4\. 全量数据清单 通用列表字段

- 数据ID、数据类型标签、风险等级、核心特征值、关联协议、关联IP、解析时间、解析状态、操作\(详情/溯源/标记/导出\)

## 5\. 九大类数据明细字段（表格最终列）

### 5\.1 网络协议

- 协议名称、协议版本、通信方向、报文长度、会话时长、报文特征、协议异常标记

### 5\.2 IP\+端口

- 源IP、目的IP、源端口、目的端口、端口服务、通信状态、IP属地、端口风险标记

### 5\.3 域名

- 根域名、子域名、解析IP、过期时间、备案状态、访问频次、恶意域名标记

### 5\.4 邮箱

- 发件邮箱、收件邮箱、邮箱协议、邮件主题、附件标识、通信时间、敏感标记

### 5\.5 账号口令（高危核心）

- 业务类型、登录账号、口令内容、加密方式、登录端口、登录IP、登录时间、弱口令判定、重复利用标记、风险评分、研判结论

### 5\.6 SSL证书

- 证书域名、序列号、颁发机构、有效期、证书指纹、过期状态、非法证书标记

### 5\.7 IMSI

- IMSI号码、运营商、归属地、关联IP、接入时间、终端特征、异常漫游标记

### 5\.8 音频

- 音频时长、采样率、格式、存储路径、识别关键词、敏感标记、播放预览

### 5\.9 视频

- 视频时长、分辨率、格式、帧截图、画面识别标签、敏感标记、播放预览

## 6\. 账号口令研判页专属字段

### 6\.1 统计字段

- 有效账号总数、明文口令数、密文口令数、弱口令数、重复账号数、高危IP数

### 6\.2 异常行为枚举

- 异地登录、高频登录、多IP共用账号、暴力破解特征、非常规端口登录

## 7\. 证书/IMSI/通信分析页字段

- 证书异常类型、证书合规结论、IMSI漫游状态、终端异常特征、协议异常统计、隐蔽端口统计、高频短连接统计

## 8\. 音视频取证页字段

- 音频波形、转写文本、关键片段标记、视频帧预览、关键画面截取、溯源链路、关联IP端口、采集时段

## 9\. 告警/研判通用字段

- 告警ID、告警时间、告警级别、告警类型、关联目标、触发规则、佐证条数、处置状态、处置记录

## 10\. 数据源监控字段

- 在线状态、数据延迟、更新频率、丢包率、采集总量、断连次数、访问频次、限流状态

> （注：部分内容可能由 AI 生成）
